import { ModuleFields } from '@hubspot/cms-components/fields';

// Reuse the existing mock-product fields so the new module
// stays 1:1 compatible with the current content model.
export { fields } from '../CeMockProductSection/fields.js';

export default ModuleFields;

